import { navbar } from './assignmentsReducer'
import { actions as a } from './assignmentsActions'

export const reducer = navbar;   // If there are multiple, combine them here

export const actions = a;
